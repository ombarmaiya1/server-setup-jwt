# server-setup-jwt
Basic express server 

## Usage

```bash
npm install server-with-jwt
```

This will scaffold a ready-to-run Express backend starter in a `backend/` folder in the directory where you ran the command.

## Manual scaffolding

```bash
npx server-with-jwt [project-name]
```
